import numpy as np

def set_array(L, rows, cols):
    if len(L) != rows * cols:
        raise ValueError("The number of elements in the list does not match the specified dimensions.")
    
    return np.array(L).reshape(rows, cols)